NODE_ENV=development
HOST=0.0.0.0
PORT=3000
JSON_LIMIT=1mb
INTEGRATION_HTTP_TIMEOUT_MS=10000
INTEGRATION_HTTP_RETRIES=2
SHUTDOWN_TIMEOUT_MS=10000

# Integration credentials should only be set in .env or a production secret manager.
# EXAMPLE_API_KEY=replace-with-your-api-key

OPEN_WEATHER_API_KEY=your-openweather-api-key